# Copiar como .env.local y pegar el valor real.
# El token se crea en Vercel: proyecto -> Settings -> Environment Variables.
# (.env.local esta en .gitignore, no se sube al repo)
BLOB_READ_WRITE_TOKEN=vercel_blob_rw_xxxxxxxxxxxxxxxxxxxxxxxx
