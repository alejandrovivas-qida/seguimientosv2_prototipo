# Copiá este archivo como .env.local y reemplazá el valor.
# El token lo sacás desde el dashboard del Blob Store en Vercel.
# Settings -> "Connect to project" o "Create token" (Read/Write).
BLOB_READ_WRITE_TOKEN=vercel_blob_rw_xxxxxxxxxxxxxxxxxxxxxxxx
